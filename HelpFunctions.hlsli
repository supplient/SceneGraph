#ifndef HELP_FUNCTIONS_HEADER
#define HELP_FUNCTIONS_HEADER

bool IsValidTexID(uint id)
{
    return id != uint(-1);
}

bool IsValidLightID(uint id)
{
    return id != uint(-1);
}

#endif//HELP_FUNCTIONS_HEADER